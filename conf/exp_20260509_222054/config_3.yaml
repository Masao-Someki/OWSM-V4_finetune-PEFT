defaults:
  - ../base_recipe_template

# Prefix-tuning baseline, valuable in low-resource and ASR tasks
exp_tag: exp_20260509_222054_prefix
method:
  type: prefix_tuning
  num_virtual_tokens: 20
  prefix_projection: True
  dropout: 0.05
