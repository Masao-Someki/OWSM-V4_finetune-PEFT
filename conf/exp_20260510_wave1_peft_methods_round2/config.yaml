# Wave 1 round-2 alias config (points to first trial semantics)
defaults:
  - ../default.yaml
  - ../dataset.yaml

lr: 5e-5
exp_tag: exp_20260510_wave1_peft_methods_round2_prefix_tuning

peft:
  type: prefix_tuning
  task_type: seq_2_seq_lm
  num_virtual_tokens: 32

model:
  _target_: src.peft_model.OWSMFinetune
  model_tag: ${base_model_tag}
  peft: ${peft}